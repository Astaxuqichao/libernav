# Simulation

Local simulation components. Production navigation packages must not depend on this directory.

- `omni_fake_node`: simulated omnidirectional base and odometry.
- `simulation_lidar`: map-based simulated lidar and point cloud.

